---
phase: 01-monorepo-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - jest.config.js
  - src/app.module.ts
  - src/main.ts
  - src/billing-links/billing-links.module.ts
  - src/billing-links/billing-links.controller.ts
  - src/billing-links/billing-links.service.ts
  - src/billing-links/billing-links.service.spec.ts
  - src/billing-links/billing-link.entity.ts
  - src/billing-links/dto/create-billing-link.dto.ts
  - src/billing-links/dto/update-billing-link.dto.ts
  - src/public-charge/public-charge.module.ts
  - src/public-charge/public-charge.controller.ts
  - src/public-charge/public-charge.service.ts
  - src/public-charge/public-charge.service.spec.ts
  - src/public-charge/dto/public-charge.dto.ts
  - src/metrics/metrics.module.ts
  - src/metrics/metrics.controller.ts
  - src/shared/auth/jwt-auth.guard.ts
  - src/shared/auth/jwt.strategy.ts
  - src/shared/correlation/correlation-id.middleware.ts
  - src/shared/idempotency/idempotency.service.ts
  - src/shared/idempotency/idempotency.service.spec.ts
  - src/shared/rate-limit/rate-limiter.middleware.ts
  - src/shared/rate-limit/rate-limiter.middleware.spec.ts
  - src/shared/pii/pii-sanitizer.ts
  - src/shared/pii/pii-sanitizer.spec.ts
autonomous: true
requirements:
  - INFRA-01
  - INFRA-03

must_haves:
  truths:
    - "npm test passes from the monorepo root using NestJS 11"
    - "npm run build succeeds from the monorepo root producing dist/"
    - "No files remain under node-api/ (directory deleted)"
    - "package.json lives at the monorepo root with @nestjs/core ^11"
    - "AppModule middleware uses forRoutes('{*splat}') syntax"
  artifacts:
    - path: "package.json"
      provides: "Root package manifest with NestJS 11 deps"
      contains: '"@nestjs/core": "^11'
    - path: "tsconfig.json"
      provides: "Root TypeScript config"
      contains: '"outDir"'
    - path: "jest.config.js"
      provides: "Root Jest config"
      contains: 'testRegex'
    - path: "src/app.module.ts"
      provides: "Root NestJS module with fixed wildcard middleware"
      contains: "{*splat}"
    - path: "src/main.ts"
      provides: "Monorepo-root NestJS bootstrap entry point"
      contains: "NestFactory.create"
  key_links:
    - from: "src/public-charge/public-charge.module.ts"
      to: "src/billing-links/billing-links.module.ts"
      via: "relative import"
      pattern: "from '\\.\\./billing-links/billing-links\\.module'"
    - from: "src/app.module.ts"
      to: "src/shared/correlation/correlation-id.middleware"
      via: "middleware registration"
      pattern: "CorrelationIdMiddleware"
---

<objective>
Upgrade NestJS from v10 to v11 and collapse the `node-api/` subdirectory into the monorepo root so that the project has a single `src/`, `package.json`, `tsconfig.json`, and `jest.config.js` at the top level. This plan keeps TypeORM in place — it is only removed after Prisma proves it works in Plan 02. The goal is a clean NestJS 11 build that passes `npm test` after every step.

Purpose: INFRA-01 requires NestJS 11 on Node 20 with the Express v5 `{*splat}` wildcard fix. INFRA-03 requires the three legacy directories (`node-api/`, `dotnet-service/`, `frontend/`) to collapse into a monorepo layout where the NestJS app sits at the root. Collapsing the directory AT THE SAME TIME as upgrading avoids two separate broken states; both changes touch the same files.

Output: Monorepo root has `src/`, `package.json`, `tsconfig.json`, `jest.config.js`, `package-lock.json`. `node-api/` directory no longer exists. `dotnet-service/` and `frontend/` remain untouched. All 4 existing spec files pass under NestJS 11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-scaffold/01-RESEARCH.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Current NestJS 10 surface area. All imports below must keep working after the upgrade. -->

Current package.json (node-api/package.json) declares:
- @nestjs/common, @nestjs/core, @nestjs/platform-express, @nestjs/jwt, @nestjs/testing: ^10.0.0
- @nestjs/passport: ^10.0.0, passport, passport-jwt (KEEP for Plan 01 — Plan 03 removes)
- @nestjs/typeorm ^10.0.0, typeorm ^0.3.17 (KEEP for Plan 01 — Plan 04 removes)
- ioredis ^5.3.2, axios ^1.6.0, class-validator ^0.14.0, class-transformer ^0.5.1
- pg ^8.11.3, uuid ^9.0.0, reflect-metadata ^0.1.13, rxjs ^7.8.1

Current AppModule (node-api/src/app.module.ts) — quote verbatim:
```typescript
import { MiddlewareConsumer, Module, NestModule } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
// ... imports
@Module({
  imports: [
    TypeOrmModule.forRoot({
      type: 'postgres',
      url: process.env.DATABASE_URL ?? 'postgresql://postgres:postgres@localhost:5432/witetec',
      entities: [BillingLink],
      synchronize: false,
    }),
    BillingLinksModule, PublicChargeModule, MetricsModule,
  ],
})
export class AppModule implements NestModule {
  configure(consumer: MiddlewareConsumer) {
    consumer.apply(CorrelationIdMiddleware).forRoutes('*');  // <-- BREAKS in NestJS 11
    consumer.apply(RateLimiterMiddleware).forRoutes('v1/public/charge/:linkId');
  }
}
```

Current tsconfig.json (node-api/tsconfig.json) — quote verbatim:
```json
{
  "compilerOptions": {
    "module": "commonjs",
    "declaration": true,
    "removeComments": true,
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "allowSyntheticDefaultImports": true,
    "target": "ES2021",
    "sourceMap": true,
    "outDir": "./dist",
    "baseUrl": "./",
    "incremental": true,
    "skipLibCheck": true,
    "strictNullChecks": true,
    "noImplicitAny": false,
    "strictBindCallApply": false,
    "forceConsistentCasingInFileNames": false,
    "noFallthroughCasesInSwitch": false
  }
}
```

Current jest.config.js (node-api/jest.config.js):
- rootDir: '.'
- testRegex: '.*\\.spec\\.ts$'
- transform: ts-jest for .ts/.js
- testEnvironment: 'node'
- globals.ts-jest.tsconfig: './tsconfig.json'

NestJS 11 breaking change applicable to this codebase (only one — audit confirms):
- `forRoutes('*')` must become `forRoutes('{*splat}')`
- `forRoutes('v1/public/charge/:linkId')` is safe (no wildcard).

Imports that rely on relative paths AFTER the move must still resolve. Directory depth does NOT change:
- `node-api/src/billing-links/*` -> `src/billing-links/*` (same depth relative to src/)
- `node-api/src/shared/auth/jwt-auth.guard.ts` -> `src/shared/auth/jwt-auth.guard.ts`
All internal relative imports keep working because relative paths between src/* files are unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Move node-api/ contents to monorepo root and delete the subdirectory</name>
  <read_first>
    - node-api/package.json (top-level scripts and deps)
    - node-api/tsconfig.json
    - node-api/jest.config.js
    - node-api/src/app.module.ts (to confirm no hardcoded node-api/ paths in imports)
    - node-api/.env.example (if exists)
  </read_first>
  <files>
    - package.json (moved from node-api/package.json)
    - package-lock.json (moved from node-api/package-lock.json)
    - tsconfig.json (moved from node-api/tsconfig.json)
    - jest.config.js (moved from node-api/jest.config.js)
    - src/** (moved from node-api/src/**)
  </files>
  <action>
    Perform a file-tree move, NOT a copy-and-edit. Using git-friendly operations:

    1. From project root `C:\tmp\features-wite-tec`:
       - `mv node-api/package.json package.json`
       - `mv node-api/package-lock.json package-lock.json`
       - `mv node-api/tsconfig.json tsconfig.json`
       - `mv node-api/jest.config.js jest.config.js`
       - `mv node-api/src src`
       - If `node-api/.env.example` exists: `mv node-api/.env.example .env.example` ONLY if root `.env.example` does not already exist; otherwise leave for manual merge by developer and note in task output.
       - `mv node-api/node_modules node_modules` IF node_modules exists (optional — `npm install` in next task regenerates).

    2. Delete the now-empty `node-api/` directory: `rmdir node-api` (must be empty at this point).

    3. Do NOT modify any file contents in this task. Relative imports inside `src/` continue to resolve because depth is preserved. The only path change is the project root location of `package.json`, `tsconfig.json`, `jest.config.js`.

    4. Do NOT move `dotnet-service/`, `dotnet-service-tests/`, `frontend/`, `db/`, `docker-compose.yml`, `.planning/`, `README.md`, `CLAUDE.md`, or `specs/`. They stay at the root.

    5. Verify no stale `node-api/` references remain in repo files — grep for the literal string `node-api/` across `package.json`, `tsconfig.json`, `jest.config.js`, `src/**/*.ts`, `CLAUDE.md`, and `docker-compose.yml`. If found in code (not in docs), list them and STOP — a file has a hardcoded path that needs updating. Docs references in `README.md` or `CLAUDE.md` can stay for now but flag them in the output.

    Reasoning: The NestJS 10 test suite still works from this new location because (a) `jest.config.js` uses `rootDir: '.'` which is now the monorepo root, and (b) `testRegex` matches any `*.spec.ts` under `src/`. No config changes needed yet.
  </action>
  <verify>
    <automated>test ! -d node-api && test -f package.json && test -f tsconfig.json && test -f jest.config.js && test -d src/billing-links && test -d src/public-charge && test -d src/shared/auth && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ls node-api` exits with non-zero (directory does not exist)
    - `ls package.json` at monorepo root exits 0
    - `ls src/app.module.ts` at monorepo root exits 0
    - `ls src/billing-links/billing-link.entity.ts` at monorepo root exits 0
    - `ls src/shared/auth/jwt-auth.guard.ts` at monorepo root exits 0
    - `ls dotnet-service` exits 0 (unchanged)
    - `ls frontend` exits 0 (unchanged)
    - `grep -r "node-api/" src/ || true` returns empty (no source files reference the old path)
    - Docker compose file at root is unchanged: `grep -q "./db/migrations:/docker-entrypoint-initdb.d" docker-compose.yml` exits 0
  </acceptance_criteria>
  <done>All files from node-api/ are at the monorepo root in their correct locations. node-api/ directory no longer exists. No source file references the old path.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Upgrade NestJS core to v11 (install new versions, keep TypeORM + Passport for now) and verify peer-dep resolution</name>
  <read_first>
    - package.json (at monorepo root, freshly moved)
    - package-lock.json (at monorepo root)
    - src/app.module.ts
    - src/billing-links/billing-links.module.ts (uses JwtModule.register, PassportModule — must keep working)
    - src/metrics/metrics.module.ts (uses PassportModule — must keep working)
    - .planning/phases/01-monorepo-scaffold/01-RESEARCH.md (Pattern 6 — wildcard route fix)
  </read_first>
  <files>
    - package.json
    - package-lock.json
    - src/app.module.ts
  </files>
  <action>
    From the monorepo root (`C:\tmp\features-wite-tec`):

    1. Edit `package.json` to bump the following `dependencies` and `devDependencies` to NestJS 11 target versions. DO NOT REMOVE Passport or TypeORM — those are removed in later plans. Set these exact versions (verified via npm registry on 2026-04-17):
       - `"@nestjs/common": "^11.1.19"`
       - `"@nestjs/core": "^11.1.19"`
       - `"@nestjs/platform-express": "^11.1.19"`
       - `"@nestjs/jwt": "^11.0.2"`
       - `"@nestjs/passport": "^11.0.5"` (KEEP Passport intact so tests continue running; v11.0.5 is compat with NestJS 11 common)
       - `"@nestjs/typeorm": "^11.0.0"` (KEEP TypeORM — v11 compat with NestJS 11 runtime)
       - `"@nestjs/testing": "^11.1.19"` (in devDependencies)
       - `"typescript": "^5.7.3"` (required for NestJS 11; bump from ^5.3.3)
       - `"@types/node": "^20.17.10"` (bump; Node 20 minimum)
       - `"reflect-metadata": "^0.2.2"` (NestJS 11 requires ^0.2)

       Keep these exact versions for other packages (no change needed):
       - `passport`, `passport-jwt`, `@types/passport-jwt`, `typeorm`, `pg`, `ioredis`, `axios`, `uuid`, `class-validator`, `class-transformer`, `rxjs`, `jest`, `ts-jest`, `ts-node-dev`

    2. Add an `"engines"` block to package.json enforcing Node 20+:
       ```json
       "engines": {
         "node": ">=20.0.0"
       }
       ```

    3. Run `npm install` at the monorepo root. Expected: a fresh `package-lock.json` generated, NO peer-dependency errors for `@nestjs/common@^11`. If npm reports peer-dep ERRORs (not warnings), capture the output and STOP. If only WARN lines appear for deprecated packages or optional peer deps, continue.

    4. Edit `src/app.module.ts` line 25 — change the wildcard route registration. The ONLY line to change is the `CorrelationIdMiddleware` registration:
       - FROM: `consumer.apply(CorrelationIdMiddleware).forRoutes('*');`
       - TO:   `consumer.apply(CorrelationIdMiddleware).forRoutes('{*splat}');`
       Do NOT touch the `RateLimiterMiddleware.forRoutes('v1/public/charge/:linkId')` line — it has no wildcard.

    5. Run `npm run build` at the monorepo root. It must compile to `dist/` without TypeScript errors. If TS errors appear (e.g., subtle type changes in NestJS 11 decorators), fix them by adding explicit type annotations — do NOT disable strict checks or add `any` blindly.

    6. Run `npm test` at the monorepo root. All existing specs (`billing-links.service.spec.ts`, `public-charge.service.spec.ts`, `idempotency.service.spec.ts`, `rate-limiter.middleware.spec.ts`, `pii-sanitizer.spec.ts`) must pass. If any test fails due to NestJS 11 runtime changes, fix the MINIMAL spec surface area — do not rewrite the business logic yet (that belongs in Plan 03).

    Reasoning: Keeping TypeORM + Passport at this stage means the app still boots and tests stay green. The only NestJS 11-specific code change in the codebase is the one wildcard route string per research audit. Later plans remove TypeORM (after Prisma works) and Passport (after CanActivate guard is ready).
  </action>
  <verify>
    <automated>npm install --no-audit --no-fund --silent && npm run build && npm test -- --runInBand --forceExit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` at root: `grep -q '"@nestjs/core": "\^11' package.json` exits 0
    - `package.json` contains `"engines"` with `"node": ">=20.0.0"`: `grep -q '">=20' package.json` exits 0
    - `package-lock.json` at root contains `"@nestjs/core"` at major version 11: `grep -A1 '"node_modules/@nestjs/core"' package-lock.json | grep -q '"version": "11' ` exits 0
    - `src/app.module.ts` contains `forRoutes('{*splat}')`: `grep -q "forRoutes('{\\*splat}')" src/app.module.ts` exits 0
    - `src/app.module.ts` contains NO `forRoutes('*')`: `grep -q "forRoutes('\\*')" src/app.module.ts` exits 1 (not found)
    - `npm run build` exits 0
    - `npm test -- --runInBand --forceExit` exits 0
    - Test output reports AT LEAST the 5 existing spec files passing (billing-links.service, public-charge.service, idempotency.service, rate-limiter.middleware, pii-sanitizer)
  </acceptance_criteria>
  <done>NestJS 11 installed, TypeScript compiles, all existing tests pass, wildcard route fixed. TypeORM and Passport intentionally retained for later plans.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → npm registry | npm install resolves packages from public registry; transitive deps not audited in this plan |
| monorepo root filesystem | Move operations can accidentally destroy files; must verify before `rmdir` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | package.json | mitigate | Pin exact target versions (`^11.1.19` etc.) derived from npm registry verification in 01-RESEARCH.md; `npm install` produces a fresh lockfile so the exact dependency tree is recorded and committed. |
| T-01-02 | Information Disclosure | .env.example (if moved) | mitigate | Task 1 moves `.env.example` ONLY if root version does not exist; no secret values ever live in `.env.example` (placeholders only per repo convention). |
| T-01-03 | Denial of Service | npm install | accept | Registry unavailability is a dev-machine concern, not a production threat. Retry is the standard recovery. |
| T-01-04 | Elevation of Privilege | npm install (supply chain) | accept | Transitive dep auditing is out of scope for Phase 1 (this is infra scaffolding, not production deploy). AVAIL-05 pipeline in Plan 04 adds `npm audit` enforcement if needed. |
| T-01-05 | Tampering | src/app.module.ts (wildcard change) | mitigate | The exact string replacement (`forRoutes('*')` → `forRoutes('{*splat}')`) is the only wildcard route in the codebase per research audit; grep verification in acceptance criteria ensures no stray `forRoutes('*')` remain. |

</threat_model>

<verification>
Phase-level checks after this plan completes:
- `npm test -- --runInBand --forceExit` from monorepo root passes all existing specs.
- `npm run build` exits 0.
- `node --version` reports v20 or higher (developer must have Node 20+ installed).
- `git status` shows `node-api/` deletion, root-level `package.json`/`tsconfig.json`/`jest.config.js` additions, and `src/app.module.ts` modification (wildcard fix only).
</verification>

<success_criteria>
- `npm test` passes at the monorepo root (addresses ROADMAP success criterion 1 partially — full criterion completes in Plan 04).
- Directory layout matches research target: `src/` + `package.json` + `tsconfig.json` at root (addresses ROADMAP success criterion 3).
- No `forRoutes('*')` remains in the codebase.
- `node-api/` subdirectory no longer exists.
- `@nestjs/core` is at major version 11 in `package-lock.json`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-scaffold/01-01-SUMMARY.md` documenting:
- Final `package.json` dependency versions (snapshot of `^11` packages and anything upgraded from `^5.3.3` TypeScript etc.)
- Any NestJS 10→11 runtime surprises encountered and how they were resolved (tests, build errors)
- Confirmation that `node-api/` is gone and all expected files live at the root
- Any stray `node-api/` references still present in docs (README.md, CLAUDE.md) for Plan 04 follow-up
</output>
